import styled from 'styled-components';
const Wrapper = styled.div`
  .filter-time {
    display: flex;
    gap: 10px;
    .filter-time__item {
      display: flex;
      align-items: center;
      gap: 10px;
    }
  }
`;

export default Wrapper;
